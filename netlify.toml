[build]
  command = "pnpm build"

[build.environment]
  NODE_VERSION = "18.19.1"

# Runtime Next.js officiel : gère le SSR, les routes API et la publication.
# (Remplace l'ancienne config Vue/Vite : publish=dist + redirect SPA /index.html.)
[[plugins]]
  package = "@netlify/plugin-nextjs"
